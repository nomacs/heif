# ** File generated automatically, do not modify **

SET(HEIF_INCLUDE_DIRS @HEIF_INCLUDE_DIRS@)
SET(HEIF_LIBS @HEIF_LIBS@) 
SET(HEIF_BUILD_PATH @CMAKE_CURRENT_BINARY_DIR@) 
SET(HEIF_PROJECT_NAME @PROJECT_NAME@)

IF(HEIF_INCLUDE_DIRS)
	SET(HEIF_FOUND true)
ELSE()
	SET(HEIF_FOUND "HEIF_FOUND-NOTFOUND")
ENDIF()
